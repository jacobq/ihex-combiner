namespace IntelHexCombiner;
public class Program
{
    public static void Main(string[] args)
    {
        // For now this program only calculates a CRC and writes it to a hex file (does not combine hex files yet)
        ProgramSetCrc.Main(args);
    }
}

